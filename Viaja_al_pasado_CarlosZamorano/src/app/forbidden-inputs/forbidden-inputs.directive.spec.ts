import { ForbiddenInputsDirective } from './forbidden-inputs.directive';

describe('ForbiddenInputsDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenInputsDirective();
    expect(directive).toBeTruthy();
  });
});
