import { OrdenarPrecioPipe } from './ordenar-precio.pipe';

describe('OrdenarPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
